{
  "name": "youtube-mcp-cli",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/mcp && npm run build -w packages/cli",
    "dev:mcp": "npm run build -w packages/core && npm run dev -w packages/mcp",
    "dev:cli": "npm run build -w packages/core && npm run dev -w packages/cli"
  },
  "engines": {
    "node": ">=18"
  }
}
